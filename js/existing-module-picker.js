(() => {const list=document.getElementById('list');if(!list||document.getElementById('existing-module-picker'))return;const box=document.createElement('div');box.id='existing-module-picker';box.style.cssText='margin:12px 0;padding:10px;border:1px solid #36516f;border-radius:10px;background:#081426';box.innerHTML='<label class="field" style="margin-top:0"><span>📚 Module personalizate existente</span><select id="existing-module-select"><option value="">Selectează un modul pentru editare</option></select></label><div style="display:flex;gap:7px;margin-top:8px"><button id="existing-edit" class="button cyan" type="button">✏️ Editează modulul</button><button id="existing-duplicate" class="button" type="button">⧉ Duplică modulul</button></div><small class="muted">Lista se actualizează automat din modulele încărcate din backend.</small>';list.parentElement.insertBefore(box,list);const sel=document.getElementById('existing-module-select');const sync=()=>{const current=sel.value;sel.innerHTML='<option value="">Selectează un modul pentru editare</option>'+[...list.querySelectorAll('[data-select]')].map(b=>`<option value="${b.dataset.select}">${b.querySelector('strong')?.textContent||b.dataset.select}</option>`).join('');if([...sel.options].some(o=>o.value===current))sel.value=current;};const open=()=>{if(!sel.value)return;list.querySelector(`[data-select="${CSS.escape(sel.value)}"]`)?.click();};document.getElementById('existing-edit').onclick=open;document.getElementById('existing-duplicate').onclick=()=>{open();setTimeout(()=>document.getElementById('module-safe-duplicate')?.click(),100)};new MutationObserver(sync).observe(list,{childList:true,subtree:true});sync();})();
